cmake_minimum_required(VERSION 2.8)


configure_file(ptrarith.encoded.veri.cfg ptrarith.encoded.veri.cfg)
configure_file(ptrarith.encoded.cover.cfg ptrarith.encoded.cover.cfg)
configure_file(ptrarith.plain.cover.cfg ptrarith.plain.cover.cfg)
