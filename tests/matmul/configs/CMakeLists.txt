cmake_minimum_required(VERSION 2.8)


configure_file(matmul.encoded.veri.cfg matmul.encoded.veri.cfg)
configure_file(matmul.encoded.cover.cfg matmul.encoded.cover.cfg)
configure_file(matmul.plain.cover.cfg matmul.plain.cover.cfg)
