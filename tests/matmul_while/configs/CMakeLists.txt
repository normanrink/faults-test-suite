cmake_minimum_required(VERSION 2.8)


configure_file(matmul_while.encoded.veri.cfg matmul_while.encoded.veri.cfg)
configure_file(matmul_while.encoded.cover.cfg matmul_while.encoded.cover.cfg)
configure_file(matmul_while.plain.cover.cfg matmul_while.plain.cover.cfg)
