cmake_minimum_required(VERSION 2.8)


configure_file(bubblesort.random-binary.encoded.veri.cfg bubblesort.random-binary.encoded.veri.cfg)
configure_file(bubblesort.random-binary.encoded.cover.cfg bubblesort.random-binary.encoded.cover.cfg)
configure_file(bubblesort.random-binary.plain.cover.cfg bubblesort.random-binary.plain.cover.cfg)
