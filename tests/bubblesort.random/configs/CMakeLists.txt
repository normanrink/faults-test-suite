cmake_minimum_required(VERSION 2.8)


configure_file(bubblesort.random.encoded.veri.cfg bubblesort.random.encoded.veri.cfg)
configure_file(bubblesort.random.encoded.cover.cfg bubblesort.random.encoded.cover.cfg)
configure_file(bubblesort.random.plain.cover.cfg bubblesort.random.plain.cover.cfg)
