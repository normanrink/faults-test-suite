cmake_minimum_required(VERSION 2.8)


configure_file(des.encoded.veri.cfg des.encoded.veri.cfg)
configure_file(des.encoded.cover.cfg des.encoded.cover.cfg)
configure_file(des.plain.cover.cfg des.plain.cover.cfg)
