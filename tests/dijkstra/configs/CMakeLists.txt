cmake_minimum_required(VERSION 2.8)


configure_file(dijkstra.encoded.veri.cfg dijkstra.encoded.veri.cfg)
configure_file(dijkstra.encoded.cover.cfg dijkstra.encoded.cover.cfg)
configure_file(dijkstra.plain.cover.cfg dijkstra.plain.cover.cfg)
