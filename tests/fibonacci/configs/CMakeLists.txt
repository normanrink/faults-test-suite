cmake_minimum_required(VERSION 2.8)


configure_file(fibonacci.encoded.veri.cfg fibonacci.encoded.veri.cfg)
configure_file(fibonacci.encoded.cover.cfg fibonacci.encoded.cover.cfg)
configure_file(fibonacci.plain.cover.cfg fibonacci.plain.cover.cfg)
