cmake_minimum_required(VERSION 2.8)


configure_file(crc.encoded.veri.cfg crc.encoded.veri.cfg)
configure_file(crc.encoded.cover.cfg crc.encoded.cover.cfg)
configure_file(crc.plain.cover.cfg crc.plain.cover.cfg)
