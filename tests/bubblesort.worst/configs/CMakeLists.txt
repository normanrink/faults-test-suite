cmake_minimum_required(VERSION 2.8)


configure_file(bubblesort.worst.encoded.veri.cfg bubblesort.worst.encoded.veri.cfg)
configure_file(bubblesort.worst.encoded.cover.cfg bubblesort.worst.encoded.cover.cfg)
configure_file(bubblesort.worst.plain.cover.cfg bubblesort.worst.plain.cover.cfg)
