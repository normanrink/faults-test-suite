cmake_minimum_required(VERSION 2.8)


configure_file(quicksort.random.encoded.veri.cfg quicksort.random.encoded.veri.cfg)
configure_file(quicksort.random.encoded.cover.cfg quicksort.random.encoded.cover.cfg)
configure_file(quicksort.random.plain.cover.cfg quicksort.random.plain.cover.cfg)
