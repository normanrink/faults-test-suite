cmake_minimum_required(VERSION 2.8)


configure_file(array_sum.encoded.veri.cfg array_sum.encoded.veri.cfg)
configure_file(array_sum.encoded.cover.cfg array_sum.encoded.cover.cfg)
configure_file(array_sum.plain.cover.cfg array_sum.plain.cover.cfg)
