# Values on commented lines are set by a top-level
# config file.

# [DEFAULT]
# The 'DEFAULT' section is ignored by the coverage tool.
# Use the 'DEFAULT' section to define names that should be
# interpolated in susequent sections.
# pin_command:  ${PIN_COMMAND}
# bfi_so_path:  ${BFI_SO_PATH}
# binaries:     ${BINARIES_FOLDER}
# suite:        ${SUITE_FOLDER}/encoded.cover

# [SUITE array_sum.encoded.cover]
# folder:     %(suite)s
# pin:        %(pin_command)s
# bfi:        %(bfi_so_path)s

[TEST array_sum.encoded.cover]
profile:    cover
faults:     (RREG: 600), (WREG: 600),
            (RVAL: 100), (WVAL: 100),
            (RADDR: 100), (WADDR: 100),
            (TXT: 600), (CF: 600)
warm-ups:   20
binary:     %(binaries)s/array_sum/array_sum.encoded
function:   ___enc_sum
output:     cs.out
expected:   0
detected:   2
reference:  %(binaries)s/array_sum/array_sum.out
folder:     %(suite)s/array_sum.encoded.cover/

