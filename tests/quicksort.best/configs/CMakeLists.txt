cmake_minimum_required(VERSION 2.8)


configure_file(quicksort.best.encoded.veri.cfg quicksort.best.encoded.veri.cfg)
configure_file(quicksort.best.encoded.cover.cfg quicksort.best.encoded.cover.cfg)
configure_file(quicksort.best.plain.cover.cfg quicksort.best.plain.cover.cfg)
