cmake_minimum_required(VERSION 2.8)


configure_file(matmul_overflow.encoded.veri.cfg matmul_overflow.encoded.veri.cfg)
configure_file(matmul_overflow.encoded.cover.cfg matmul_overflow.encoded.cover.cfg)
configure_file(matmul_overflow.plain.cover.cfg matmul_overflow.plain.cover.cfg)
