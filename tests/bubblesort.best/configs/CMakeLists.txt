cmake_minimum_required(VERSION 2.8)


configure_file(bubblesort.best.encoded.veri.cfg bubblesort.best.encoded.veri.cfg)
configure_file(bubblesort.best.encoded.cover.cfg bubblesort.best.encoded.cover.cfg)
configure_file(bubblesort.best.plain.cover.cfg bubblesort.best.plain.cover.cfg)
