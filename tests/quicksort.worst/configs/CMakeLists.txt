cmake_minimum_required(VERSION 2.8)


configure_file(quicksort.worst.encoded.veri.cfg quicksort.worst.encoded.veri.cfg)
configure_file(quicksort.worst.encoded.cover.cfg quicksort.worst.encoded.cover.cfg)
configure_file(quicksort.worst.plain.cover.cfg quicksort.worst.plain.cover.cfg)
