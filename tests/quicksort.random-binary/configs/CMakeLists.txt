cmake_minimum_required(VERSION 2.8)


configure_file(quicksort.random-binary.encoded.veri.cfg quicksort.random-binary.encoded.veri.cfg)
configure_file(quicksort.random-binary.encoded.cover.cfg quicksort.random-binary.encoded.cover.cfg)
configure_file(quicksort.random-binary.plain.cover.cfg quicksort.random-binary.plain.cover.cfg)
